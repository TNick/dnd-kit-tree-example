import { AppLayout } from "../layout/layout"

/**
 * The page showcasing the original example but using MUI for styling.
 */
export const MuiPage = () => {
    return (
        <AppLayout example={null} controls={null} />
    )
}
